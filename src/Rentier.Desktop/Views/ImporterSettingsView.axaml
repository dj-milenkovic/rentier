<reactive:ReactiveUserControl
    x:TypeArguments="vm:ImporterSettingsViewModel"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:reactive="http://reactiveui.net"
    xmlns:vm="using:Rentier.Desktop.ViewModels"
    xmlns:local="using:Rentier.Desktop.Converters"
    x:Class="Rentier.Desktop.Views.ImporterSettingsView"
    x:CompileBindings="False"
    x:DataType="vm:ImporterSettingsViewModel">
  <Grid ColumnDefinitions="250,*">
    <!-- Left panel: importer list -->
    <DockPanel Grid.Column="0">
      <Button DockPanel.Dock="Bottom"
              Command="{Binding AddNewCommand}"
              Content="{Binding [Importers_AddNew_Button], Source={StaticResource Localizer}}"
              HorizontalAlignment="Stretch"
              Margin="4" />
      <ListBox ItemsSource="{Binding ImporterItems}"
               SelectedItem="{Binding SelectedImporter, Mode=TwoWay}">
        <ListBox.ItemTemplate>
          <DataTemplate>
            <StackPanel>
              <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" />
              <TextBlock Text="{Binding ReportTypeDisplay}" FontSize="11" Opacity="0.7" />
            </StackPanel>
          </DataTemplate>
        </ListBox.ItemTemplate>
      </ListBox>
    </DockPanel>

    <!-- Right panel: form -->
    <ScrollViewer Grid.Column="1">
      <StackPanel Margin="16" Spacing="8">
        <TextBlock Text="{Binding [Importers_DisplayName_Label], Source={StaticResource Localizer}}" />
        <TextBox Text="{Binding DisplayName}" />

        <TextBlock Text="{Binding [Importers_ReportType_Label], Source={StaticResource Localizer}}" />
        <ComboBox ItemsSource="{Binding AvailableReportTypes}"
                  SelectedItem="{Binding ReportType, Mode=TwoWay}">
          <ComboBox.ItemTemplate>
            <DataTemplate>
              <TextBlock Text="{Binding Converter={x:Static local:ReportTypeDisplayConverter.Instance}}" />
            </DataTemplate>
          </ComboBox.ItemTemplate>
        </ComboBox>

        <TextBlock Text="{Binding [Importers_TaxpayerProfile_Label], Source={StaticResource Localizer}}" />
        <ComboBox ItemsSource="{Binding AvailableProfiles}"
                  SelectedItem="{Binding SelectedProfile, Mode=TwoWay}"
                  PlaceholderText="{Binding [Importers_NoProfile_Placeholder], Source={StaticResource Localizer}}">
          <ComboBox.ItemTemplate>
            <DataTemplate>
              <TextBlock Text="{Binding FullName}" />
            </DataTemplate>
          </ComboBox.ItemTemplate>
        </ComboBox>

        <TextBlock Text="{Binding [Importers_Mailbox_Label], Source={StaticResource Localizer}}" />
        <ComboBox ItemsSource="{Binding AvailableMailboxes}"
                  SelectedItem="{Binding SelectedMailbox, Mode=TwoWay}"
                  PlaceholderText="{Binding [Importers_NoMailbox_Placeholder], Source={StaticResource Localizer}}">
          <ComboBox.ItemTemplate>
            <DataTemplate>
              <TextBlock>
                <Run Text="{Binding Username}" /><Run Text=" @ " /><Run Text="{Binding Host}" />
              </TextBlock>
            </DataTemplate>
          </ComboBox.ItemTemplate>
        </ComboBox>

        <TextBlock Text="{Binding [Importers_FromFilter_Label], Source={StaticResource Localizer}}" />
        <TextBox Text="{Binding FromFilter}" />

        <TextBlock Text="{Binding [Importers_SubjectFilter_Label], Source={StaticResource Localizer}}" />
        <TextBox Text="{Binding SubjectFilter}" />

        <TextBlock Text="{Binding [Importers_AttachmentRegex_Label], Source={StaticResource Localizer}}" />
        <TextBox Text="{Binding AttachmentRegex}" />

        <TextBlock Text="{Binding [Importers_PaymentNotes_Label], Source={StaticResource Localizer}}" />
        <TextBox Text="{Binding PaymentNotes}"
                 AcceptsReturn="True"
                 MinLines="3" />

        <StackPanel Orientation="Horizontal" Spacing="8">
          <Button Command="{Binding SaveCommand}"
                  Content="{Binding [Importers_Save_Button], Source={StaticResource Localizer}}" />
          <Button Command="{Binding DeleteCommand}"
                  Content="{Binding [Importers_Delete_Button], Source={StaticResource Localizer}}" />
        </StackPanel>

        <TextBlock Text="{Binding ErrorMessage}"
                   Foreground="Red"
                   IsVisible="{Binding ErrorMessage, Converter={x:Static StringConverters.IsNotNullOrEmpty}}" />
        <TextBlock Text="{Binding SuccessMessage}"
                   Foreground="Green"
                   IsVisible="{Binding SuccessMessage, Converter={x:Static StringConverters.IsNotNullOrEmpty}}" />
        <ProgressBar IsIndeterminate="True"
                     IsVisible="{Binding IsLoading}"
                     Height="4" />
      </StackPanel>
    </ScrollViewer>
  </Grid>
</reactive:ReactiveUserControl>
